-- Joker handler for j_splash
-- Splash: Every played card counts in scoring
-- This is a passive effect that modifies scoring rules
return function(joker, state, ctx, CONTEXT, Scoring)
    -- Passive effect - all played cards score
    -- No direct scoring effect
end

